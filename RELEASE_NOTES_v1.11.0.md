# K2E Local v1.11.0 — Setup Audit, Calculation Checks, and Safe Backup

- Adds a local setup audit for unrealistic runtime, missing wattage, possible duplicates, unusual quantities, and commonly omitted major loads.
- Adds browser-run reference calculation checks.
- Adds versioned full local-data backup download.
- Adds import preview and confirmation before replacement.
- Creates a session recovery snapshot before import and provides one-click restoration.
- Keeps all household data local and preserves the existing calculation and storage model.
