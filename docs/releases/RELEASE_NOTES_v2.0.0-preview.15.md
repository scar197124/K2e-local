# K2E Local v2.0.0-preview.15

## Simple Mode finishing polish

- Locked the home and estimate panels to matching desktop heights.
- Kept Adjust Devices collapsed by default.
- Added a plain-language estimate-quality indicator.
- Added a local utility-data import path for XML, CSV, and JSON files.
- Imported files are read only in the browser; nothing is uploaded.
- K2E attempts to detect a direct rate or calculate an effective rate from cost and usage.
- Direct live utility-account connection remains reserved for Advanced Mode.

## Release integrity

- Synchronized `VERSION` and `manifest.json` at `2.0.0-preview.15`.
- Rotated the offline cache to `k2e-local-v2.0.0-preview.15`.
- Updated release validation to test the current Simple Mode features and deployment package.
- Updated the README current-release information.

