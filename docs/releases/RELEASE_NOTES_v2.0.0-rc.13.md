# K2E Local v2.0.0-rc.13

- Moves **+ Add device** into the Explore Your Energy tool row.
- Makes the device preset chooser a floating, height-limited panel so it no longer reduces Device Controls reading space.
- Adds a live device count to the Device Controls tab.
- Closes the picker after adding and returns the user to Device Controls.
- Adds Escape/outside-click dismissal for the picker.
- Keeps the Device Controls workspace and table height stable while devices are added.
