# K2E Local v1.12.5 — Metadata, README, and Release Consistency Polish

## Changes

- Added `og:image` dimensions, image type, secure URL, and descriptive alt text to the landing page and simulator.
- Added X/Twitter large-card metadata and image alt text.
- Added cache-busting query version `v=1.12.5` to public social-image URLs.
- Added canonical URLs and index/follow metadata consistently.
- Added a compact 1200 × 420 README banner while preserving the 1200 × 630 social-sharing image.
- Rebuilt the README around the current release, launch link, privacy boundary, deployment, and validation.
- Synchronized `VERSION`, `manifest.json`, service-worker cache naming, and the validation script to v1.12.5.
- Added a repeatable social and release-preview checklist.

## Compatibility

No calculation-engine, saved-data, storage-key, or interface-layout changes were made.
